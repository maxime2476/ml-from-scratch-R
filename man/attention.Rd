% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/38_attention.R
\name{attention}
\alias{attention}
\title{Attention par produit scalaire mis a l'echelle}
\usage{
attention(Q, K, V, mask = FALSE)
}
\arguments{
\item{Q, K, V}{matrices requetes (T_q x d_k), cles (T_k x d_k), valeurs (T_k x d_v).}

\item{mask}{logique : masque causal (position i ne voit que j <= i).}
}
\value{
liste : \code{out} (T_q x d_v), \code{weights} (T_q x T_k).
}
\description{
\eqn{\mathrm{Attn}(Q,K,V)=\mathrm{softmax}\!\bigl(QK^\top/\sqrt{d_k}\bigr)V}.
Chaque requete \eqn{q_i} interroge toutes les cles ; les poids (similarites
normalisees) melangent les valeurs. Une option de masque \strong{causal} interdit
de regarder le futur (auto-regression).
}
