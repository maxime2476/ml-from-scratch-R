% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_bagging_rf.R
\name{bagging_fit}
\alias{bagging_fit}
\title{Bagging / forêt aléatoire (bootstrap + agrégation)}
\usage{
bagging_fit(
  formula,
  data,
  method = c("class", "anova"),
  B = 100L,
  mtry = NULL,
  max_depth = 30L,
  min_split = 5L,
  min_leaf = 1L,
  seed = NULL
)
}
\arguments{
\item{formula}{formule (prédicteurs numériques).}

\item{data}{data.frame.}

\item{method}{"class" ou "anova".}

\item{B}{nombre d'arbres.}

\item{mtry}{variables candidates par split ; NULL = bagging (toutes) ;
sinon forêt aléatoire. Défaut : sqrt(p) (class), p/3 (anova).}

\item{max_depth, min_split, min_leaf}{hyperparamètres des arbres de base.}

\item{seed}{graine.}
}
\value{
objet \code{forest} : \code{trees}, \code{oob_idx}, \code{oob_error}, \code{oob_pred}, méta.
}
\description{
Ajuste B arbres (Module 8) sur des rééchantillons bootstrap ; agrège par
moyenne (régression) ou vote majoritaire (classification). Avec \code{mtry}, chaque
split ne considère qu'un sous-ensemble aléatoire de variables (forêt
aléatoire, décorrélation des arbres). Calcule l'erreur out-of-bag (éq. 9.2).
}
