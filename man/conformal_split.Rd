% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/19_conformal.R
\name{conformal_split}
\alias{conformal_split}
\title{Prédiction conforme par découpage (split conformal)}
\usage{
conformal_split(
  X_train,
  y_train,
  X_cal,
  y_cal,
  X_test,
  fit_fn,
  predict_fn,
  alpha = 0.1,
  normalize = FALSE,
  sigma_fn = NULL
)
}
\arguments{
\item{X_train, y_train}{données d'entraînement.}

\item{X_cal, y_cal}{données de calibration.}

\item{X_test}{points où prédire.}

\item{fit_fn}{fonction \verb{(X, y) -> modèle}.}

\item{predict_fn}{fonction \verb{(modèle, X) -> prédictions}.}

\item{alpha}{niveau (défaut 0.1 -> 90 \%).}

\item{normalize}{normalisation locale (défaut FALSE).}

\item{sigma_fn}{(si normalize) fonction \verb{(X) -> dispersion locale positive}.}
}
\value{
liste : \code{lower}, \code{upper}, \code{pred}, \code{qhat}.
}
\description{
Ajuste un modèle sur l'échantillon d'entraînement, calibre les scores de
non-conformité \eqn{|y-\hat\mu(x)|} sur l'échantillon de calibration, et
renvoie des intervalles \eqn{\hat\mu(x)\pm\hat q} pour \code{X_test} (Théorème 19.1).
Avec \code{normalize = TRUE}, les scores sont divisés par une dispersion locale
\eqn{\hat\sigma(x)} (intervalles de largeur variable).
}
