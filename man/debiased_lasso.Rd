% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/22_debiased_lasso.R
\name{debiased_lasso}
\alias{debiased_lasso}
\title{Lasso débiaisé / désparsifié (inférence haute dimension valide)}
\usage{
debiased_lasso(
  X,
  y,
  lambda = NULL,
  lambda_node = NULL,
  targets = NULL,
  sigma = NULL,
  level = 0.95
)
}
\arguments{
\item{X}{matrice n x p.}

\item{y}{réponse.}

\item{lambda}{pénalité du lasso principal (défaut : théorique).}

\item{lambda_node}{pénalité des lasso nodewise (défaut : théorique).}

\item{targets}{indices des coordonnées à débiaiser (défaut : toutes).}

\item{sigma}{écart-type du bruit (défaut : estimé sur les résidus du lasso).}

\item{level}{niveau de confiance des intervalles (défaut 0.95).}
}
\value{
liste : \code{estimate}, \code{se}, \code{lower}, \code{upper}, \code{beta_lasso}, \code{sigma}.
}
\description{
Corrige le biais de rétrécissement du lasso par une \strong{projection de faible
dimension} (Zhang-Zhang 2014) : pour chaque coordonnée j, on partiale les
autres covariables hors de \eqn{x_j} par un lasso « nodewise », et on utilise
le résidu \eqn{\hat\tau_j} comme score orthogonal (même idée que Neyman, M16) :
\eqn{\hat\beta^d_j=\hat\beta_j + \hat\tau_j^\top(y-X\hat\beta)/(\hat\tau_j^\top x_j)}.
Fournit des intervalles de confiance \strong{valides} même si p > n — là où le
t-test post-lasso naïf échoue (Module 14).
}
