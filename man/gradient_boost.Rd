% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_boosting.R
\name{gradient_boost}
\alias{gradient_boost}
\title{Gradient boosting (descente de gradient fonctionnelle, éq. 10.1-10.2)}
\usage{
gradient_boost(
  formula,
  data,
  loss = c("l2", "logloss"),
  M = 100L,
  nu = 0.1,
  max_depth = 3L,
  min_split = 10L,
  min_leaf = 5L,
  lambda = 0,
  newton = TRUE
)
}
\arguments{
\item{formula}{formule (prédicteurs numériques).}

\item{data}{data.frame.}

\item{loss}{"l2" (régression) ou "logloss" (classification binaire \verb{y in \{0,1\}}).}

\item{M}{nombre d'arbres.}

\item{nu}{taux d'apprentissage (shrinkage) dans (0,1].}

\item{max_depth, min_split, min_leaf}{hyperparamètres des arbres de base.}

\item{lambda}{régularisation L2 des poids de feuille (éq. 10.7).}

\item{newton}{raffiner les feuilles par le pas de Newton (défaut TRUE).}
}
\value{
objet \code{boost}.
}
\description{
Ajuste additivement M arbres aux pseudo-résidus. \code{loss = "l2"} (résidu, éq.
10.3) ou \code{"logloss"} (y - p, éq. 10.5). Avec \code{newton = TRUE}, les poids de
feuille sont ceux de Newton (éq. 10.7) — pour la log-loss, la mise à jour de
Friedman (comme \code{gbm}).
}
