% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_validation.R
\name{kfold_cv}
\alias{kfold_cv}
\title{Validation croisée K-fold générique (éq. 6.2)}
\usage{
kfold_cv(
  X,
  y,
  K = 10L,
  fit_fun = function(Xtr, ytr) solve_ls_qr(Xtr, ytr)$coefficients,
  pred_fun = function(beta, Xte) as.numeric(Xte \%*\% beta),
  seed = NULL
)
}
\arguments{
\item{X}{matrice de design (constante incluse si le modèle en a une).}

\item{y}{réponse.}

\item{K}{nombre de blocs (défaut 10).}

\item{fit_fun}{fonction \verb{(Xtr, ytr) -> modèle} (défaut : OLS via QR).}

\item{pred_fun}{fonction \verb{(modèle, Xte) -> prédictions} (défaut : OLS).}

\item{seed}{graine pour le tirage des blocs.}
}
\value{
liste : \code{cv}, \code{se}, \code{fold_errors}, \code{folds}.
}
\description{
Validation croisée K-fold générique (éq. 6.2)
}
