% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_linalg.R
\name{svd_tools}
\alias{svd_tools}
\title{Outils SVD : rang numérique, conditionnement, pseudo-inverse}
\usage{
svd_tools(X, tol = NULL)
}
\arguments{
\item{X}{matrice n x p.}

\item{tol}{seuil de rang ; par défaut \eqn{\max(n,p)\,\varepsilon\,\sigma_1}.}
}
\value{
liste : \code{d} (valeurs singulières), \code{rank}, \code{kappa}, \code{pinv}
(pseudo-inverse p x n), \code{u}, \code{v}, \code{tol}.
}
\description{
À partir de \eqn{X = U\Sigma V^T} (éq. 0.11), calcule le rang numérique, le
conditionnement \eqn{\kappa_2 = \sigma_{\max}/\sigma_{\min}} (éq. 0.3) et la
pseudo-inverse de Moore-Penrose \eqn{X^+ = V\Sigma^+ U^T} (éq. 0.12). La SVD
elle-même est déléguée à \code{svd()} (existence admise, Th. 0.4).
}
