# Fonctions élémentaires enregistrées (exp, log, sqrt, sin, cos, tanh)

Fonctions élémentaires enregistrées (exp, log, sqrt, sin, cos, tanh)

## Usage

``` r
# S3 method for class 'adnode'
Math(x, ...)
```

## Arguments

- x:

  `adnode`.

- ...:

  arguments additionnels (ignorés).

## Value

`adnode`.
