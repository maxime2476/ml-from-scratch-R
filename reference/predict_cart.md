# Prédiction d'un arbre CART

Prédiction d'un arbre CART

## Usage

``` r
predict_cart(object, newdata)
```

## Arguments

- object:

  objet `cart`.

- newdata:

  data.frame contenant les prédicteurs.

## Value

vecteur de prédictions (classes pour "class", moyennes pour "anova").
