# Softmax numeriquement stable (par ligne)

Softmax numeriquement stable (par ligne)

## Usage

``` r
softmax_rows(X)
```

## Arguments

- X:

  matrice ; @param axis 1 (par ligne, defaut).

## Value

matrice de memes dimensions, chaque ligne sommant a 1.
