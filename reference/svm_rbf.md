# Noyau gaussien (RBF) pour SVM

Noyau gaussien (RBF) pour SVM

## Usage

``` r
svm_rbf(X1, X2, gamma)
```

## Arguments

- X1, X2:

  matrices ; @param gamma largeur inverse.

## Value

matrice de noyau.
